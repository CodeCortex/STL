#include <bits/stdc++.h>
using namespace std;

int main()
{
    multimap<int, string> m;
    // in multimap, in one index you can store multiple values
    // equivalent to
    map<int, vector<string>> str;

    return 0;
}